<?php

require_once("database.php");

//Get the post request contating the TIL and save to data base

//Get the thumbs up or thumbs down and increment/decrement the TIL in the database by id

//return a the records from the database for display (paginate) if they hit index